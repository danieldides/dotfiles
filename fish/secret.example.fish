# System-specific configuration that should NOT be committed to public repositories
# Copy this file to secret.fish and customize it for each system
#
# Add system-specific environment variables here, for example:
#
# # Kubernetes configuration (system-specific)
# set -gx KUBECONFIG $HOME/.kube/config
#
# # AWS profile (system-specific)
# set -gx AWS_PROFILE your-profile
#
# # System-specific paths
# fish_add_path $HOME/.local/bin
# fish_add_path ~/.lmstudio/bin
#
# # Registry credentials (SECRET!)
# set -gx REGISTRY1_USERNAME your-username
# set -gx REGISTRY1_TOKEN your-token
#
# # PYENV_ROOT (usually system-specific)
# set -gx PYENV_ROOT $HOME/.pyenv
# fish_add_path $PYENV_ROOT/bin
